HTML (Hypertext Markup Language)

It is basically giving instruction to computer what to do.

When HTML file save you can open it in a web browser.

Computer reads HTML code from top to bottom it does the necessary work mentioned by the code.

1.) A HTML element
eg.)  p , button

2.) HTML tags
Every tag has two tags 
                                             i.) opening tag
                                             ii.) closing tag
eg.)<p></p>


3.) HTML attribute
eg.) <a href="" target=""></a>
href determines where the link will take the user
target determines wether link opens in a new tab

4.)

